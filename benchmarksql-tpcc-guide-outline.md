## BenchmarkSQL TPC-C 测试作业指导书（目录大纲）

### 1. 概述与准备
- **文档目标与读者**: 明确测试目的、读者角色（DBA/性能工程师/开发）
- **TPC-C 简介**: 业务模型、指标说明（TPS/Latency/Abort 等）
- **名词与约定**: terminals、warehouses、ramp-up、mix 比例

### 2. 安装与环境
- **获取与编译**: 源码获取方式、JDK/Gradle/Maven 要求、构建产物
- **目录结构**: `run/`、`sql.common/`、`sql.<db>/`、`src/`、`dist/`
- **推荐机器配置**
  - 单机数据库：CPU/内存/磁盘/网络建议
  - 分布式数据库：客户端与数据库侧规模、网卡与存储建议
  - 推荐并发数与仓数（terminals/warehouses）选型原则与参考表
- **系统与内核准备**: 时钟同步、文件句柄、调度与 I/O、HugePages/THP

### 3. BenchmarkSQL 的安装使用
- **数据准备与装载**: 建库建表、索引/约束、装载流程与常见参数
- **典型运行方式**: 单节点/多客户端、CLI 命令、输出目录结构
- **针对单机与分布式数据库的并发与仓数建议**
  - 单机数据库：常用 terminals 与 warehouses 组合
  - 分布式数据库：跨分片/副本拓扑下的组合与注意事项
- **运行前检查清单**: 连接连通性、账户权限、磁盘与空间、水位与限速

### 4. 兼容与非兼容数据库的测试方法
- **兼容数据库测试步骤（开箱可用）**
  - PostgreSQL / MySQL / Oracle（示例步骤与差异点清单）
  - JDBC 驱动放置、`benchmarksql.properties` 样例
- **非兼容数据库改造指引**
  - 语法/对象差异定位方法（数据类型、自增、序列、存储过程）
  - 需要修改的文件清单与改造点
    - `run/props.*` 与 `benchmarksql.properties`
    - `sql.common/*.sql` 与 `sql.<db>/*`（DDL/DML/事务语义）
    - `storedprocedures/*`（存储过程/函数实现替换）
    - 源码中与 SQL 方言相关的类（如 `src/client/` 配置）
  - JDBC 驱动与连接串格式、隔离级别与一致性设置
  - 校验与回归：装载校验、事务通过率、结果一致性

### 5. 参数优化（BenchmarkSQL 侧）
- **负载模型参数**: `terminals`、`warehouses`、`rampup`、`runMins`、事务权重
- **装载参数**: `loadWorkers`、批量提交策略、并行建索引
- **客户端 JVM 优化**: 堆大小、GC 策略、`-XX` 常用参数、GC 日志
- **Think/Keying Time**: 开关策略、业务等价与基准可比性
- **错误处理与重试**: 超时、并发异常、退避策略与统计口径
- **稳定性与预热**: 预热时长、收敛判定、结果取样窗口

### 6. 资源监控与观测（基准机与数据库侧）
- **基准机监控**
  - CPU/内存/GC/网络/磁盘：`sar`、`pidstat`、`iostat`、`dstat`、`vmstat`、`jstat`
  - 采样频率、时间同步（NTP/Chrony）、数据留存策略
- **数据库侧监控**
  - OS 指标与数据库内部指标（连接、锁、事务、缓冲命中、延迟分位）
  - Prometheus/Grafana 方案、Node/DB Exporter、可观测性面板建议
- **瓶颈定位路径**: CPU 饱和、IO 等待、锁争用、网络带宽/RTT、GC 停顿

### 7. 配置文件与参数说明
- **`benchmarksql.properties` 字段分组说明**
  - 连接配置：JDBC URL、驱动类、账户、连接池相关
  - 负载配置：`warehouses`、`terminals`、`rampup`、`runMins`、权重
  - 装载配置：`loadWorkers`、装载并发与批次、数据校验开关
  - 结果与日志：输出目录、明细与聚合、错误日志
- **`run/` 目录脚本与变量**：启动脚本、环境变量、参数覆盖顺序
- **`sql.common/` 与 `sql.<db>/`**：可替换模块、方言定制位置与边界
- **结果输出与报告**：原始日志、聚合结果、可视化建议

### 8. 测试流程与交付物
- **端到端流程**：准备→装载→预热→压测→收敛→复核→出报告
- **报告模板**：环境/版本、参数、监控图、指标表、结论与建议
- **复现实验**：脚本化、版本固定、随机种子与配置快照

### 9. 附录
- **并发与仓数选型参考表**：单机/分布式建议区间与注意事项
- **常见问题与故障排查**：连接失败、DDL 差异、超时与中止率
- **参考资料**：官方仓库、TPC-C 规范要点、相关博文/最佳实践

